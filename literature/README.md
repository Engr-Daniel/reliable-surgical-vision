# Literature

Maintain the evidence base here. `literature-map.csv` is the structured index; `references.bib` stores citation metadata.

Recommended extraction fields include research question, task, dataset, model, shift, reliability method, metrics, network variables, principal finding, limitations, and relevance to Tracks A–C.
